@using MichaelKjellander.Models.Wordpress

@if (Posts != null)
{
    @for (int i = 0; i < Posts.Count; i++)
    {
        <PostBox Post="Posts[i]"/>
    }
}

@code {
    [Parameter] public IList<WpPost>? Posts { get; set; }
}