<svg xmlns="http://www.w3.org/2000/svg" class="inline icon icon-tabler" width="@(Size ?? 20)" height="@(Size ?? 20)" viewBox="0 0 24 24" stroke-width="1.5" stroke="#2c3e50" fill="none" stroke-linecap="round" stroke-linejoin="round">
    @ChildContent
</svg>

@code {
    [Parameter] public RenderFragment ChildContent { get; set; }
    [Parameter] public int? Size { get; set; } = 20;
}